- Fix a crash when closing the last window tile in Exposé with ⌘W (or moving it to another workspace). Removing the last tile left the grid empty and a stale index could trap during the dismissal animation.
