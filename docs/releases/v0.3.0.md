- Reorganize settings around features into four pages with compact controls.
- Add translations for Japanese, Simplified Chinese, Korean, Spanish, French, and German.
- Add a welcome tour on first launch and looping drawn animations for each settings section.
- Drop the Accessibility permission. Option+number selection in the window overview is gone; plain number and letter keys still work.
- Turn quick window switching on by assigning a shortcut and off by clearing it. Existing ⌥Tab setups keep working.
- Make the Try buttons in settings work: the workspace grid stays open, and App Exposé shows the app you were using.
- Pick excluded apps from a searchable list, and keep preview images when switching to manual updates.
- Keep ⌘R and ⌘, as fixed keys inside the workspace grid, with the key hints always visible.
